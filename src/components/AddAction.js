import React from "react";

const AddLearnMoreButton = (props) => {
  return (
    <button size="small" onClick={props.learnMore}>
      Learn More
    </button>
  );
};

export default AddLearnMoreButton;
